"""Unit tests for the Push Reliability report/maintenance cron script.

Makes this directory a package so its `conftest` does not collide with
`tests/notification/conftest.py` as a top-level module during mypy's analysis
(`tests/integration` is a package for the same reason).
"""
